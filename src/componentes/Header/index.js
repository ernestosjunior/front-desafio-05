import "./style.css";

const Header = () => {
  return (
    <header>
      <img />
      <h1>Pizza Pizzaria & Delivery</h1>
      <button>Logout</button>
    </header>
  );
};

export default Header;
